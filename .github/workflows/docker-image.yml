name: Deploy .NET no Google Cloud Run

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: Trilha-Esporte
    env:
      IMAGE_NAME: "us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/dotnet-repo/trilha-esporte:v1.0-${{ github.sha }}"

    steps:
      # 🔹 Checkout do código
      - name: Checkout do código
        uses: actions/checkout@v4

      # 🔹 Autenticar no Google Cloud
      - name: Autenticar no Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # 🔹 Configurar autenticação do Docker para o Artifact Registry
      - name: Configurar autenticação do Docker
        run: gcloud auth configure-docker us-central1-docker.pkg.dev --quiet

      # 🔹 Verificar a versão do Docker e se buildx está instalado
      - name: Verificar a versão do Docker
        run: |
          docker --version
          docker buildx version || echo "docker buildx não está instalado"

      # 🔹 Garantir que estamos usando o build tradicional (docker build)
      - name: Forçar uso do docker build (sem buildx)
        run: |
          docker build --version || echo "docker build não está instalado"

      # 🔹 Construir a imagem Docker
      - name: Construir a imagem Docker
        run: |
          echo "Construindo a imagem Docker..."
          docker build -t ${IMAGE_NAME} .

      # 🔹 Enviar a imagem para o Google Artifact Registry
      - name: Enviar imagem para o Google Artifact Registry
        run: |
          echo "Enviando a imagem para o Google Artifact Registry..."
          docker push ${IMAGE_NAME}

      # 🔹 Fazer o deploy no Cloud Run
      - name: Deploy no Cloud Run
        run: |
          echo "Realizando deploy no Cloud Run..."
          gcloud run deploy trilha-esporte \
            --image=${IMAGE_NAME} \
            --region=us-central1 \
            --platform=managed \
            --port=8080 \
            --allow-unauthenticated \
            --timeout=2000s \
            --set-env-vars="DB_USER=${{ secrets.DB_USER }},DB_PASSWORD=${{ secrets.DB_PASSWORD }},DB_HOST=${{ secrets.DB_HOST }},DB_PORT=${{ secrets.DB_PORT }},DB_NAME=${{ secrets.DB_NAME }}"

      # 🔹 Mostrar URL do serviço
      - name: Exibir URL do serviço
        run: |
          echo "Obtendo
